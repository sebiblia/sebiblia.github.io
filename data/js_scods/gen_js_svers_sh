#!/bin/bash

CURR_DIR=$(pwd)
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "GENERATING "${1}"_SVERSES.js using "${BASH_SOURCE[0]}

cd $SCRIPT_DIR


IN_DIR="../txt_tabs"
OUT_DIR="."
SLQ_FILE="./get_SREF_tab.sql3"

fin=${IN_DIR}/${1}_txt.tab
fout=${OUT_DIR}/${1}_sref.tab
js_out=${OUT_DIR}/${1}_SVERSES.js

echo "Generating "${fout}" from "${fin}

cp ${fin} in.tab
sed -i 's/:/|/g' in.tab

rm ${fout}
rm out.tab
sqlite3 < ${SLQ_FILE}
mv out.tab ${fout}
rm in.tab

echo "Generating "${js_out}" from "${fout}

node print_js_svers.js ${fout} ${1}


cd ${CURR_DIR}

